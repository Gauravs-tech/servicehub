spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/servicehub_db
    username: servicehub
    password: servicehub123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      jdbc:
        time_zone: UTC
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: localhost
      port: 6379